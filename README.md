#Incog Adventure Application

##Application setup

To start working with the application you will have to install its dependencies with [Composer](https://getcomposer.org/)

    $ composer install

## Testing

### Run spec tests

To run the specs test run the following command

    $ bin/phpspec run

